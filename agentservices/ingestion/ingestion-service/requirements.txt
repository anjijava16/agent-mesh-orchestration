# Ingestion Service Dependencies
# Focused on document processing, parsing, chunking, embedding, and indexing

# ---- Web Framework ---------------------------------------------------
fastapi>=0.115,<1               # Web framework
uvicorn[standard]>=0.32,<1      # ASGI server

# ---- Task Queue ------------------------------------------------------
celery[redis]>=5.5,<6
redis>=5.2,<6

# ---- Database Drivers ------------------------------------------------
asyncpg>=0.30,<1            # Async PostgreSQL
psycopg2-binary>=2.9,<3     # Sync PostgreSQL (legacy)
psycopg[binary]>=3.2,<4     # Sync PostgreSQL (for Celery & SQLAlchemy 2.0)
SQLAlchemy[asyncio]>=2.0,<3 # ORM with async support
alembic>=1.14,<2            # Database migrations

# ---- Vector Store Clients --------------------------------------------
opensearch-py[async]>=2.8,<3    # OpenSearch client
motor>=3.6,<4                   # Async MongoDB (Atlas Vector Search)
pymongo>=4.10,<5                # Sync MongoDB (for Celery workers)
pinecone-client>=5.0,<6         # Pinecone vector database (optional)

# ---- Embeddings ------------------------------------------------------
openai>=1.69,<2                 # OpenAI embeddings API
langchain-openai>=0.3,<1        # LangChain OpenAI wrapper
litellm>=1.58,<2                # Unified LLM/embedding API (no proxy extras)

# ---- Document Parsers ------------------------------------------------
pypdf>=5.1,<6                   # PDF parsing
python-docx>=1.1,<2             # DOCX parsing
openpyxl>=3.1,<4                # Excel parsing
beautifulsoup4>=4.12,<5         # HTML parsing
lxml>=5.3,<6                    # XML/HTML parser
python-magic>=0.4,<1            # File type detection
pytesseract>=0.3,<1             # OCR wrapper for Tesseract

# ---- Object Storage --------------------------------------------------
boto3>=1.36,<2                  # AWS S3 / MinIO client
aioboto3>=13,<14                # Async S3 client

# ---- Utilities -------------------------------------------------------
pydantic>=2.10,<3               # Data validation
pydantic-settings>=2.7,<3       # Settings management
structlog>=24,<25               # Structured logging
httpx>=0.28,<1                  # Async HTTP client
tenacity>=9.0,<10               # Retry logic
python-dotenv>=1.0,<2           # .env support
pybreaker>=1.0,<2               # Circuit breaker pattern
deepagents>=0.2.4,<0.3          # DeepAgents framework (used by backend modules)

# ---- Observability ---------------------------------------------------
opentelemetry-api>=1.29,<2
opentelemetry-sdk>=1.29,<2
opentelemetry-instrumentation-celery>=0.50b0
openinference-instrumentation-openai>=0.1.15
